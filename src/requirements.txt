# VAR 2026 Digital Twin BTS — Pipeline Requirements
# Install: pip install -r requirements.txt

# Core dependencies (must match 3DGS requirements)
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
plyfile>=1.0.0
tqdm>=4.66.0
opencv-python-headless>=4.8.0
scipy>=1.10.0
Pillow>=9.0.0

# Optional but recommended
rich>=13.0.0
